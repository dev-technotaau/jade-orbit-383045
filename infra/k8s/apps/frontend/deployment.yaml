# ============================================================
# Frontend — Argo Rollout (Canary with Prometheus analysis)
# Next.js 16 standalone output
# ============================================================
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: frontend
  namespace: hire-adda
  labels:
    app: frontend
    tier: app
    app.kubernetes.io/name: frontend
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: hire-adda
spec:
  replicas: 2
  revisionHistoryLimit: 2
  progressDeadlineSeconds: 600
  selector:
    matchLabels:
      app: frontend
  strategy:
    canary:
      stableService: frontend
      canaryService: frontend-canary
      maxSurge: 1
      maxUnavailable: 1
      scaleDownDelaySeconds: 30
      trafficRouting:
        nginx:
          stableIngress: frontend-ingress
      steps:
        - setWeight: 20
        - pause: { duration: 60s }
        - analysis:
            templates:
              - templateName: frontend-success-rate
              - templateName: frontend-canary-comparison
        - setWeight: 50
        - pause: { duration: 120s }
        - analysis:
            templates:
              - templateName: frontend-success-rate
              - templateName: frontend-canary-comparison
        - setWeight: 100
  template:
    metadata:
      labels:
        app: frontend
        tier: app
      annotations:
        # `tmp` is an EmptyDir scratch — see backend/deployment.yaml.
        backup.velero.io/backup-volumes-excludes: tmp
    spec:
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: frontend
          image: ghcr.io/dev-technotaau/hire-adda-frontend:f62f03f70cde4c467e3bd6822f3cc26b68ee5cd5
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: NODE_ENV
              value: 'production'
            # NEXT_PUBLIC_* vars are baked at build time in the CD pipeline
            # Only server-side runtime vars needed here:
            - name: BACKEND_INTERNAL_URL
              value: 'http://nginx:80/api/v1'
            - name: BFF_SECRET
              valueFrom:
                secretKeyRef:
                  name: backend-secrets
                  key: BFF_SECRET
            # Bearer token gating /api/indexnow-ping. BOTH ends need it: CD sends
            # it (cd.yml:302, from secrets.INDEXNOW_PING_TOKEN) and this pod
            # validates it (app/api/indexnow-ping/route.ts:21). Without it here
            # the route 503s before reading the header, so every post-deploy ping
            # to Bing/Yandex was silently dropped — and cd.yml's ping step runs
            # with `continue-on-error`, so nothing ever surfaced the failure.
            # Already sealed in backend-secrets:131; this only wires it in.
            - name: INDEXNOW_PING_TOKEN
              valueFrom:
                secretKeyRef:
                  name: backend-secrets
                  key: INDEXNOW_PING_TOKEN
            # BFF auth cookie settings (matches Docker Compose)
            - name: COOKIE_ACCESS_MAX_AGE_DAYS
              value: '7'
            - name: COOKIE_REFRESH_MAX_AGE_DAYS
              value: '30'
            # OpenTelemetry
            - name: OTEL_ENABLED
              value: 'true'
            - name: OTEL_SERVICE_NAME
              value: 'hire-adda-frontend'
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: 'http://otel-collector.monitoring.svc.cluster.local:4318/v1/traces'
          resources:
            requests:
              memory: '384Mi'
              cpu: '150m'
            limits:
              memory: '768Mi'
              cpu: '750m'
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 128Mi
      imagePullSecrets:
        - name: ghcr-credentials
      terminationGracePeriodSeconds: 15
