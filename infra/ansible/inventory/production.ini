# Hire Adda — Production VPS Inventory
# Hostinger VPS (16GB RAM, Ubuntu 22.04 LTS)

[k3s_server]
# NEW VPS (cutover 2026-04-29). Old IP 148.230.67.210 decommissioned.
# SSH user changed from `root` to `deploy` — root logins trigger
# fail2ban → IP-level timeout.
hireadda ansible_host=145.223.19.154 ansible_user=deploy ansible_ssh_private_key_file=~/.ssh/hireadda_deploy

[all:vars]
ansible_python_interpreter=/usr/bin/python3
