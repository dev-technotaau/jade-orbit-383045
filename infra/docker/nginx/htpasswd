# Nginx basic auth for protected subdomains (logs.hireadda.in → OpenSearch Dashboards)
# Regenerate with: docker run --rm httpd:alpine htpasswd -nbB admin 'YOUR_PASSWORD'
#
# IMPORTANT: This file is synced to VPS by deploy-vps.yml (rsync).
# After changing, redeploy or manually copy + nginx reload on VPS.
#
# Current credentials: admin / (same as Grafana admin password in .env)
admin:$2y$05$SpnyvRNLTvB0NiIXGgVSzOOzqOUzxivXuxHMsd6oxjlFqwXcNXXoa
