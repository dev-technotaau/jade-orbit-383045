# ===================================
# Application Configuration
# ===================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
NEXT_PUBLIC_SUPPORT_EMAIL=support@hireadda.in

# ===================================
# Authentication (OAuth & Security)
# ===================================
# Google
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# LinkedIn
NEXT_PUBLIC_LINKEDIN_CLIENT_ID=
# Cloudflare Turnstile (CAPTCHA)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
# WebAuthn (Passkeys)
NEXT_PUBLIC_WEBAUTHN_RP_ID=localhost

# Note: JWT_SECRET and COOKIE_SECRET are backend-only and must NOT be exposed here.

# ===================================
# BFF (Backend-For-Frontend) — server-only, NOT prefixed with NEXT_PUBLIC_
# ===================================
# Internal backend URL for server-side BFF proxy calls (falls back to NEXT_PUBLIC_API_URL)
BACKEND_INTERNAL_URL=http://localhost:5000/api/v1
# Shared secret for CSRF bypass on BFF → backend calls (must match backend BFF_SECRET)
BFF_SECRET=your-bff-secret-key-min-32-characters-long-hexstring
# Cookie maxAge (days) — must match backend COOKIE_ACCESS_MAX_AGE_DAYS / COOKIE_REFRESH_MAX_AGE_DAYS
COOKIE_ACCESS_MAX_AGE_DAYS=7
COOKIE_REFRESH_MAX_AGE_DAYS=30

# ===================================
# Analytics & Monitoring
# ===================================
# Sentry (Frontend Project DSN)
NEXT_PUBLIC_SENTRY_DSN=
# Google Analytics 4
NEXT_PUBLIC_GA_ID=
# Google Tag Manager
NEXT_PUBLIC_GTM_ID=
# Facebook Pixel
NEXT_PUBLIC_FB_PIXEL_ID=
# Microsoft Clarity (free heatmaps + session recording)
#   Project ID from https://clarity.microsoft.com/projects
NEXT_PUBLIC_CLARITY_ID=
# LinkedIn Insight Tag (B2B retargeting + conversion tracking)
#   Partner ID from https://www.linkedin.com/campaignmanager > Account Assets > Insight Tag
NEXT_PUBLIC_LINKEDIN_PARTNER_ID=
# Contentsquare (formerly Hotjar — Contentsquare acquired Hotjar in Oct 2023
# and migrated accounts onto their unified UXA tracker; legacy Hotjar IDs
# no longer issue new tags). Workspace key is the 13-char hex from the
# Contentsquare install snippet — the value between `/uxa/` and `.js` in
# `https://t.contentsquare.net/uxa/<key>.js`.
NEXT_PUBLIC_CONTENTSQUARE_ID=
# Pinterest Tag
NEXT_PUBLIC_PINTEREST_TAG_ID=
# Reddit Pixel — advertiser ID from ads.reddit.com
NEXT_PUBLIC_REDDIT_PIXEL_ID=
# X (Twitter) Pixel — universal website tag from ads.x.com
NEXT_PUBLIC_TWITTER_PIXEL_ID=
# TikTok Pixel — from ads.tiktok.com
NEXT_PUBLIC_TIKTOK_PIXEL_ID=
# Quora Pixel — from ads.quora.com
NEXT_PUBLIC_QUORA_PIXEL_ID=
# Microsoft Bing UET (Microsoft Ads / Microsoft Audience Network)
NEXT_PUBLIC_BING_UET_TAG_ID=
# Snap Pixel — Snapchat Ads
NEXT_PUBLIC_SNAP_PIXEL_ID=
# PostHog (product analytics + session replay + feature flags)
#   Project API key + host. Defaults to us.i.posthog.com.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
# Cloudflare Web Analytics manual beacon — token from dash.cloudflare.com
#   > Web Analytics > Manage site > Reveal site token.
NEXT_PUBLIC_CF_BEACON_TOKEN=
# Adobe Experience Platform Launch (paid — leave empty if not using)
#   Full Launch library URL: https://assets.adobedtm.com/<container>/<env>/launch-<hash>.min.js
NEXT_PUBLIC_ADOBE_LAUNCH_URL=

# Site last-modified timestamp (ISO 8601). Leave empty locally — the
# CI pipeline auto-populates this from `git log -1 --format=%cI` so
# every deploy stamps the site with the real commit date. Used by
# <meta name="last-modified">, article:modified_time, og:updated_time,
# Dublin Core DC.date.modified, and JSON-LD dateModified on the homepage.
NEXT_PUBLIC_SITE_LAST_MODIFIED=
# OpenTelemetry (Tracing) — server-only, NOT prefixed with NEXT_PUBLIC_
OTEL_SERVICE_NAME=hire-adda-frontend
OTEL_EXPORTER_OTLP_ENDPOINT=https://api.honeycomb.io/v1/traces
OTEL_EXPORTER_OTLP_HEADERS=x-honeycomb-team=your-honeycomb-api-key

# ===================================
# Payments (Phase 2)
# ===================================
NEXT_PUBLIC_RAZORPAY_KEY_ID=

# ===================================
# Firebase (Analytics & FCM)
# ===================================
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=
# Firebase Realtime Database (for online presence)
NEXT_PUBLIC_FIREBASE_DATABASE_URL=
# Firebase Cloud Messaging (VAPID key for web push)
NEXT_PUBLIC_FIREBASE_VAPID_KEY=

# ===================================
# Feature Flags
# ===================================
NEXT_PUBLIC_ENABLE_MOCK_DATA=false
NEXT_PUBLIC_MAINTENANCE_MODE=false

# ===================================
# Public Assets & CDN
# ===================================
# Cloudinary
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# R2 Storage
NEXT_PUBLIC_R2_PUBLIC_URL=https://assets.hireadda.in

# ===================================
# Web Push Notifications
# ===================================
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key-here

# ===================================
# Geocoding & Maps
# ===================================
NEXT_PUBLIC_NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org

# ===================================
# IndexNow — instant indexing (Bing / Yandex / Naver / Seznam)
# ===================================
# Site key — must match the public file at /public/<KEY>.txt. Used by
# the frontend's lib/indexnow.ts when constructing the keyLocation
# URL on outbound pings. Default ships out-of-box.
INDEXNOW_KEY=c43d83f87126d18729b99c85048ac0f8

# Bearer token gating /api/indexnow-ping. CI deploy step + backend
# bulk-ping callers send `Authorization: Bearer $INDEXNOW_PING_TOKEN`.
# Generate via:  openssl rand -hex 32
# Then mirror the value as a GitHub Actions secret named
# INDEXNOW_PING_TOKEN so cd.yml can use it.
INDEXNOW_PING_TOKEN=CHANGE_ME_64_char_hex_token_via_openssl_rand_hex_32
