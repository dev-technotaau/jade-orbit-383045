import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Saved Jobs',
};

export default function CandidateSavedJobsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
