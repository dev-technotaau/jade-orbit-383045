import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Verifications',
};

export default function AdminVerificationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
